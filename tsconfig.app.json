{
  "extends": "@vue/tsconfig/tsconfig.dom.json",
  "include": ["env.d.ts", "src/**/*", "src/**/*.vue"],
  "exclude": ["src/**/__tests__/*"],
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    
    // ========== 路径别名配置 ==========
    // 配置 @/ 别名指向 src 目录，与 vite.config.ts 保持一致
    // 使用示例：import { xxx } from '@/utils/xxx'
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },
    
    // ========== 核心类型检查 ==========
    // 启用严格模式：在编译时发现类型错误，而不是运行时
    // 包括：noImplicitAny, strictNullChecks, strictFunctionTypes 等
    // 这是 TypeScript 最重要的特性，帮助我们在开发阶段发现错误
    "strict": true,
    
    // ========== 基础代码质量检查 ==========
    // 确保函数所有代码路径都有返回值（避免忘记 return）
    "noImplicitReturns": true,
    
    // 确保 switch 语句完整，避免遗漏 case（防止 bug）
    "noFallthroughCasesInSwitch": true,
    
    // ========== 开发体验优化 ==========
    // 启用增量编译，加快后续编译速度
    "incremental": true,
    
    // 生成源码映射文件，方便调试
    "sourceMap": true
  }
}
